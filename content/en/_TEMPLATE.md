---
# ===== Submission Template (copy this file, rename it, then fill in) =====
# Required fields:
#   id        English slug, unique across the whole site (e.g. hk-basics-one-chart)
#   title     Article title
#   subtitle  Column tag · Subtitle (use "L0/L1…" to mark hierarchy)
#   date      Publish date, YYYY-MM-DD
#   section   Column — allowed values listed below
# Optional fields:
#   lang      Language; defaults to zh (can be omitted)
#
# Allowed section values:
#   insurance  港险学院 (HK Insurance Academy)
#   guardian   守护觉醒 (Guardian's Awakening)
#   trust      资产地图·信托 (Asset Map · Trust)
#   gold       资产地图·黄金 (Asset Map · Gold)
#   emerging   资产地图·新型 (Asset Map · Emerging)
#   arkPilot   方舟视点 (Ark Insights)
#
# ===== Body writing (Markdown is supported and will be rendered as rich text on the site) =====
# - Start each sub-section with ## ; every ## becomes a content block with a gold sub-heading
# - Inside ## you can use standard Markdown:
#   **bold**  *italic*  [link](https://example.com)
#   Unordered lists use - or * ; ordered lists use 1. 2.
#   > blockquote ; `inline code` ; ```code block``` ; use --- for a divider
#
# Note: leave a blank line before and after lists, quotes, and code blocks, or they won't be parsed as block elements.

id: your-unique-id
title: Article Title
subtitle: Column Tag · Subtitle
date: 2026-08-01
section: insurance
lang: en
---

## 1. Sub-section Title

Body paragraph goes here. You can use **bold**, *italic*, and [linked text](https://fin-ark.example.com).

- List item one
- List item two
- List item three

> This is a blockquote — good for a pull quote or a warning.

## 2. Another Sub-section

Leave a blank line between paragraphs to start a new one. Code block example:

```js
const x = 1; // demo only
```
