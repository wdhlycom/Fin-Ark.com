---
# ===== 投稿模板（复制本文件，改名后填写）=====
# 必填字段：
#   id        英文 slug，全站唯一（如 hk-basics-one-chart）
#   title     文章标题
#   subtitle  栏目标签 · 副标题（可用「L0/L1…」标层级）
#   date      发布日期 YYYY-MM-DD
#   section   板块，可选值见下
# 可选字段：
#   lang      语言，默认 zh（可省略）
#
# section 可选值：
#   insurance  港险学院
#   guardian   守护觉醒
#   trust      资产地图·信托
#   gold       资产地图·黄金
#   emerging   资产地图·新型
#   arkPilot   方舟视点
#
# ===== 正文写法（支持 Markdown，会被网页美化为富文本）=====
# - 用 ## 开头写小节标题，每个 ## 对应文章里一段带金色小标题的内容块
# - ## 以下的正文支持标准 Markdown：
#   **加粗**  *斜体*  [链接](https://example.com)
#   无序列表用 - 或 * ；有序列表用 1. 2.
#   > 引用块 ；`行内代码` ；```代码块``` ；分隔线用 ---
#
# 注意：Markdown 列表/引用/代码块前后需空一行，否则不会被识别为块级元素。

id: your-unique-id
title: 文章标题
subtitle: 栏目标签 · 副标题
date: 2026-08-01
section: insurance
lang: zh
---

## 一、小节标题

这里是正文段落，可以写 **加粗**、*斜体*，以及[带链接的文字](https://fin-ark.example.com)。

- 列表项一
- 列表项二
- 列表项三

> 这是一段引用，适合放金句或警示。

## 二、另一个小节

段落之间空一行即自动分段。代码块示例：

```js
const x = 1; // 仅作演示
```
